#include <stdio.h>
int main (){
  char aux;

  printf("Digite um caracter: ");
  scanf("%c", &aux);
  printf("%d", aux);
  /*
  1.Não foi um erro de execução, o programa apenas imprime na tela o numero correspondente
  a letra na tabela ASCII
  */
  return 0;
}
